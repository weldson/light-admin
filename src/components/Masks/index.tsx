// import InputMask from 'react-input-mask';

// interface PhoneInputProps {
//   value: string;
//   onChanges: (value: string) => void;
// }

// export const PhoneInput = ({ value, onChanges }: PhoneInputProps) => (
//   <InputMask mask="(99) 99999-9999" value={value} onChange={onChanges} />
// );
